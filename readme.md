# PYTK - Python Toolkit by One Level Studio

## Production
```
pip install -U git+https://github.com/OneLevelStudio/PYTK
```

## Development
For convenience, run `_venv.bat` to automatically create `_venv` environment first.

Then, at root (directory `PYTK`, not `pytk`), run:
```
pip install -U .
```